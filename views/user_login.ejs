<!DOCTYPE html>
<html lang="en">
<head>
    <meta charset="UTF-8">
    <meta name="viewport" content="width=device-width, initial-scale=1.0">
    <link rel="stylesheet" href="styles/bootstrap.css">
    <title>User Login</title>
    <style>
        body{
            background-color: #C7C8CC;
        }
    </style>
</head>
<body>
    <div class="container d-flex align-items-center justify-content-center" style="height: 100vh;">
        <div>
            <h2 style="text-align: center;">Login</h2>
            <% if (passwordErr && passwordErr.length > 0) { %>
                <div class="alert alert-danger" role="alert">
                    <strong style="text-align: center;">Username or Password Incorrect</strong>
                </div>
            <% } %>
            <form action="/user-login" method="post" class="form-group">
                <input class="form-control m-1"  type="username" name="username" placeholder="Username" required pattern="^\S+$">
                <input class="form-control m-1"  id="password" type="password" name="password" placeholder="Password" required>
                <input class="btn btn-info m-1" type="submit" value="Login">
            </form>
            <a style="text-align: center;" href="/sign-up">New user? sign up here</a>
        </div>
    </div>
</body>
</html>